#include <chowdsp_logging/chowdsp_logging.h>
#include <CatchUtils.h>

TEST_CASE ("Logging Test", "[common][logging]")
{
    // juce::Logger::setCurrentLogger (new chowdsp::Logger {});
    // juce::Logger::writeToLog ("test");

    // spdlog::info ("test");
    // spdlog::warn ("warn");
    // spdlog::error ("error");

    // spdlog::shutdown();
}
